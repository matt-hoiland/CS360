#include <iostream>
#include <stdexcept>
#include <string>

#include <unistd.h>

using namespace std;

int main(int argc, char** argv) {
	cout << "This is the part when..." << endl;

	bool debug = false;

	int option;
	while ((option = getopt(argc, argv, "d")) != -1) {
		switch (option) {
		case 'd' :
			debug = true;
			break;
		}
	}

	int port;
	try {
		port= stoi(string(argv[optind]));
	} catch (invalid_argument& ex) {

	} catch (out_of_range& ex) {

	}
	string dir = argv[optind + 1];
	return 0;
}
